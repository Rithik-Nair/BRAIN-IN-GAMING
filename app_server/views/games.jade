extends layout

block content
  h1(style="margin-top:100px") Available Games
  .game-list
    each game in games
      .game-item
        .game-box
          h2= game.name
          p= game.description
          button(onclick="window.location.href='/#{game.name.toLowerCase().replace(/\s+/g, '')}'") Play #{game.name}
